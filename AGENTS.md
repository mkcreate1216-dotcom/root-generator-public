# プロジェクト運用ルール (AGENTS.md)

## 作業完了時のコミットメッセージ提案
- 実装・修正などの作業が完了した際は、必ずターンの最後に**日本語1行のコミットメッセージ**（Conventional Commits形式）を考案して提案してください。
- `git commit` コマンド形式ではなく、**コミットメッセージの文章単体**のみを提示してください。
- 詳細は `.agents/skills/commit-message-helper/SKILL.md` を参照してください。

## LPのサンプル表示（「例:」タグ）に関する絶対禁止事項
- LP下部の「例:」に並ぶ旅行名（「京都 2泊3日」「北海道ドライブ」「金沢 週末ひとり旅」等）は、**ユーザーへの単なる入力例提示（静的テキスト・非クリッカブルなピル表示）**です。
- **絶対にクリック可能（`<button>` タグやリンク、`cursor-pointer`、`data-sample-trip` 属性等）にしたり、クリック時に旅行名を入力・作成開始するようなイベントリスナーを追加してはなりません。**
- 過去にAIエージェントが「利便性向上」と誤認して勝手にボタン化・インタラクティブ化し、手動でリバートされた経緯があります。UIデザインの調整やリファクタリング、機能追加の際も、この部分をボタン化・クリッカブル化することは厳禁です。

